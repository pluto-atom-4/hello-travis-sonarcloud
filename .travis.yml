language: java
sudo: false
install: true

addons:
  sonarcloud:
    organization: "pluto-atom-4-github"

jdk: openjdk10

script:
  - mvn clean verify

cache:
  directories:
    - '$HOME/.m2/repository'
